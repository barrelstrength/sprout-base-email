{% import "_includes/forms" as forms %}

<input type="hidden" name="sproutBaseSettingsType" value="{{ className(settings) }}">

{{ forms.textField({
    label: "Plugin Name"|t('sprout-base-email'),
    instructions: "Intuitive, human-readable plugin name for the end user."|t('sprout-base-email'),
    id: 'pluginNameOverride',
    name: 'pluginNameOverride',
    value: settings.pluginNameOverride,
    autofocus: true,
    first: true,
    errors: ''
}) }}
