<!DOCTYPE html>
<html lang="{{ currentSite.language }}">
<head>
    <title>{{ email.subjectLine }} - {{ siteName }}</title>
</head>
<body>

<h1>{{ email.subjectLine }}</h1>

{% if email.defaultBody|length %}
    <p>{{ email.defaultBody|markdown }}</p>
{% endif %}

</body>
</html>